apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: xcodereleases-telegram-bot-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  tls:
    - hosts:
        - xcodereleases-telegram-bot.bearologics.cloud
      secretName: xcodereleases-telegram-bot
  rules:
    - host: xcodereleases-telegram-bot.bearologics.cloud
      http:
        paths:
          - path: /
            backend:
              serviceName: xcodereleases-telegram-bot
              servicePort: 3000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: xcodereleases-telegram-bot-deployment
  labels:
    app: xcodereleases-telegram-bot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: xcodereleases-telegram-bot
  template:
    metadata:
      annotations:
        linkerd.io/inject: enabled
      labels:
        app: xcodereleases-telegram-bot
    spec:
      containers:
        - name: xcodereleases-telegram-bot
          image: <IMAGE>
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: xcodereleases-telegram-bot-secret
      imagePullSecrets:
        - name: registry-bearologics-cloud
---
apiVersion: v1
kind: Service
metadata:
  name: xcodereleases-telegram-bot
spec:
  selector:
    app: xcodereleases-telegram-bot
  ports:
    - port: 3000
      targetPort: 3000
